Add three features, an operational playbook, and documentation updates to this notification service. Create a team of 4 agents to work in parallel on items 1-4. Each agent should work in its own git worktree to avoid conflicts.

1. Auth middleware — Create src/middleware/auth.ts. Validate an X-API-Key header on all /webhooks routes. Valid keys: ["notify-dev-key", "notify-prod-key"]. Return 401 JSON error for missing or invalid keys. Write unit tests in tests/auth.test.ts.

2. Rate limiter — Create src/services/rate-limiter.ts. In-memory sliding window: max 100 requests per minute per sourceId. Export createRateLimiter(maxRequests, windowMs) that returns Express middleware. Apply to POST /webhooks/notify only. Return 429 with a Retry-After header when exceeded. Write unit tests in tests/rate-limiter.test.ts.

3. Stats endpoint — Create src/routes/stats.ts. GET /stats returns notification counts grouped by channel and priority for the last hour. Add a getNotificationStats(sinceMinutes) function to src/storage/sqlite.ts. Register the route in server.ts. Write unit tests in tests/stats.test.ts.

4. On-call playbook — Create docs/oncall-playbook.md. Read tests/acceptance.test.ts to understand the three features being built. For each feature (auth middleware, rate limiter, stats endpoint), write a runbook entry with: what it does, success/error responses, how to diagnose failures, and rollback steps. This is the on-call engineer's reference — write it from the API contract, not from implementation.

After all three features are implemented, wire the auth middleware and rate limiter into server.ts. The on-call playbook runs in parallel with the feature work — it doesn't depend on the feature agents and they don't depend on it.

After wiring is complete:

5. Librarian — Update CLAUDE.md to reflect the new features. Add the auth middleware, rate limiter, and stats endpoint to the API section. Update the architecture diagram to include new files (src/middleware/auth.ts, src/services/rate-limiter.ts, src/routes/stats.ts). The docs should match the current state of the code. This task is blocked until wiring is done.

6. Product owner — Review the notification dashboard (src/public/index.html) from a user's perspective after wiring. Write docs/ux-spec.md with specific UI improvements: what to change, why, and acceptance criteria for each change. Cover error states for auth (401), rate limiting (429), and stats display. This is the spec — not code. This task is blocked until wiring is done.

7. Dashboard dev — Read docs/ux-spec.md and implement the PO's requirements in src/public/index.html. Match the acceptance criteria exactly. This task is blocked by #6 — the PO spec must exist before you start coding.

Optional — cross-model review: If GitHub Copilot CLI is installed (`copilot --version`), run /crossmodel-review on src/middleware/auth.ts, src/services/rate-limiter.ts, and src/routes/stats.ts to get a second opinion from Google and OpenAI models. Reason over the feedback — fix real issues, document why you disagree with false positives. Skip this step if Copilot CLI is not available.

Definition of done: run npm run verify — type check AND all tests must pass, including tests/acceptance.test.ts which contains pre-written acceptance tests that validate the exact behavior specified above. Do not modify acceptance.test.ts.
